import "SplikX"

lexer = SplikX.NewLexer('Print()')

lexer.Skip('\s+')
lexer.AddRule('[0-9]+', 'INTEGER')
lexer.AddRule('[a-zA-Z_][a-zA-Z_0-9]*', 'IDENTIFIER')
lexer.AddRule('\(', 'LPAREN')
lexer.AddRule('\)', 'RPAREN')

tokens = lexer.Tokenize()

parser = SplikX.NewParser(tokens)
