func get_factors(int num) -> array[int] {
    factors = int{}
    i = 1
    while i < num {
        if num % i == 0 {
            factors.add(i)
        }

        i++
    }

    return factors
}

func main() -> int {
    print("Factors of 10: " + to_string(get_factors(10)))
    return 0
}
